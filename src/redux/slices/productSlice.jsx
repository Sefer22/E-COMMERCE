import { createSlice } from '@reduxjs/toolkit'

const initalState = {
    product: [],
    selectedProduct: {}
}

export const productSlice = createSlice({
    name: "product",
    initialState,
    reducers: {

    },
    extraReducers: {

    }
})